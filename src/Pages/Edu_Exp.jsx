import React from "react";
import styles, { layout } from "../style";

const Edu_Exp = () => {
  return (
    <div className={`${styles.flexStart} ${styles.paddingX}`}>
      <div className={styles.boxWidth}>hello</div>
    </div>
  );
};

export default Edu_Exp;
